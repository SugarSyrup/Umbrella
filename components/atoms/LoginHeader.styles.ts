import styled from "styled-components";
import { CSSProperties } from "styled-components";

export const LoginHeader = styled.span<CSSProperties>`
    font-size:36px;
    font-weight:bolder;
    margin-bottom:20px;
`;